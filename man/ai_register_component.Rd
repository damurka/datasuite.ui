% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kit-ai-bridge.R
\name{ai_register_component}
\alias{ai_register_component}
\title{Make an output reachable by the AI}
\usage{
ai_register_component(
  session,
  output_id,
  type = c("chart", "table"),
  data,
  about = NULL,
  id = sub("-$", "", session$ns(""))
)
}
\arguments{
\item{session}{The module's session.}

\item{output_id}{The output's full id (\code{session$ns("table")}), so the page can find it.}

\item{type}{\code{"chart"} or \code{"table"}.}

\item{data}{A function (or reactive) returning the data frame the output shows. It is only called when the AI asks
for the data, never to fill in the state.}

\item{about}{What the output is, for the app's AI: \code{NULL}, a list, or a function (or reactive) returning one --
e.g. \code{list(kind = "coverage", options = list(indicator = "anc4"))}. Passed to DataSuite as it is.}

\item{id}{The component's id; by default the module path (\code{session$ns("")} without its trailing \code{-}).}
}
\value{
\code{NULL}, invisibly.
}
\description{
Registers a chart or table so DataSuite's chat can list it, read the data it shows and screenshot it (see
\code{docs/AI-BRIDGE.md}). \code{\link[=cd_plot_server]{cd_plot_server()}} does this for every chart; call it for other outputs, e.g. a table, from
their module server.
}
