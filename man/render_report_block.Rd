% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-builder.R
\name{render_report_block}
\alias{render_report_block}
\title{Draw one chart or table of a report}
\usage{
render_report_block(context, block, i18n = NULL, design = NULL)
}
\arguments{
\item{context}{A report context (\code{\link[=report_context]{report_context()}}), or what \code{\link[=as_report_context]{as_report_context()}} turns into one.}

\item{block}{A chart or table block (see the top of this file for its fields). A \code{region} of \code{"@report"} is the
report's region: the caller puts it in first (\code{\link[=report_resolve_block]{report_resolve_block()}}).}

\item{i18n}{An object with a \code{t(key)} method for translated labels, or \code{NULL} for English.}

\item{design}{The report's design: its palette colours the chart's series (see \code{\link[=report_themes]{report_themes()}}). \code{NULL} keeps the
chart's own colours.}
}
\value{
\verb{list(type = "plot", value = <ggplot>)}, \verb{list(type = "table", value = <flextable>)}, or
\verb{list(type = "error", message = <text>)} when it cannot be drawn with this dataset.
}
\description{
Draw one chart or table of a report
}
