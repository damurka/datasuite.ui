% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-deck.R
\name{export_deck}
\alias{export_deck}
\title{Export a slide deck}
\usage{
export_deck(
  context,
  project,
  file,
  format = c("pptx", "pdf"),
  i18n = NULL,
  progress = NULL,
  converter = c("auto", "powerpoint", "libreoffice")
)
}
\arguments{
\item{context}{A report context (\code{\link[=report_context]{report_context()}}), or what \code{\link[=as_report_context]{as_report_context()}} turns into one.}

\item{project}{The deck (see the top of \code{R/report-deck.R}): \code{kind = "deck"}, \code{name}, \code{design}, \code{region}, \code{slides}.}

\item{file}{The file to write.}

\item{format}{\code{"pptx"} or \code{"pdf"}.}

\item{i18n}{An object with a \code{t(key)} method, or \code{NULL}.}

\item{progress}{A function called with a number between 0 and 1 as the export advances, or \code{NULL}.}

\item{converter}{What makes the PDF: \code{"auto"} (PowerPoint, else LibreOffice), \code{"powerpoint"} or \code{"libreoffice"}.}
}
\value{
\code{file}, invisibly, with attribute \code{converter} (what made the PDF; \code{NULL} for a PowerPoint file).
}
\description{
Draws every chart and table of a deck from the loaded data and writes it as a PowerPoint file, or as a PDF made from
that file (by PowerPoint or LibreOffice, see \code{\link[=report_deck_converter]{report_deck_converter()}}). Every item is placed at its box: text as
text boxes in the design's fonts and colours, charts as editable drawings (or pictures, when formatted as pictures or
when they cannot be drawn as shapes), tables as PowerPoint tables, pictures as pictures. The slides' speaker notes
are kept.
}
