% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-export.R
\name{report_final_pages}
\alias{report_final_pages}
\title{The pages of a report as they will be printed}
\usage{
report_final_pages(context, project, i18n = NULL, dpi = 60, progress = NULL)
}
\arguments{
\item{context}{A report context (\code{\link[=report_context]{report_context()}}), or what \code{\link[=as_report_context]{as_report_context()}} turns into one.}

\item{project}{The report: \code{list(name, design, cover, blocks)} (see \code{\link[=report_presets]{report_presets()}}), or a slide deck.}

\item{i18n}{An object with a \code{t(key)} method, or \code{NULL}.}

\item{dpi}{Resolution of the page pictures.}

\item{progress}{A function called with a number between 0 and 1 as the export advances, or \code{NULL}.}
}
\value{
\verb{list(pages = <character vector of PNG files>, converter, pdf = <path>)}. The files are in a temporary folder
the caller removes.
}
\description{
Writes the Word file, makes the PDF from it and renders each page as a picture: what the builder's "Final pages" view
shows. Needs Microsoft Word or LibreOffice (\code{\link[=report_converter]{report_converter()}}). For a slide deck, the PowerPoint file and its PDF
(PowerPoint or LibreOffice, \code{\link[=report_deck_converter]{report_deck_converter()}}): one picture per slide.
}
