% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-options-facet.R
\name{chart_facet_info}
\alias{chart_facet_info}
\title{Whether a chart is split into panels, and how}
\usage{
chart_facet_info(plot)
}
\arguments{
\item{plot}{A ggplot (anything else gives \code{NULL}).}
}
\value{
\code{NULL} when the chart has no panels. Otherwise a list with
\itemize{
\item \code{type}: \code{"wrap"} or \code{"grid"};
\item \code{ncol}, \code{nrow}: the number of columns and rows of panels as drawn (integer; \code{NA} when the plot cannot be built and
the facet does not say);
\item \code{scales}: \code{"fixed"}, \code{"free"}, \code{"free_x"} or \code{"free_y"};
\item \code{strip_position}: \code{"top"}, \code{"bottom"}, \code{"left"} or \code{"right"} (for \code{facet_grid()}, where the column labels are, or
the row labels when it only has rows);
\item \code{panels}: the number of panels (integer, \code{NA} when the plot cannot be built).
}
}
\description{
Tells whether a ggplot is split into panels (\code{facet_wrap()} or \code{facet_grid()}) and how they are laid out now, so an
editor can offer the panel options of \code{\link[=cd_chart_options]{cd_chart_options()}} (\code{facet_ncol}, \code{facet_nrow}, \code{facet_scales},
\code{strip_position}) only for charts that have panels.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point() + facet_wrap(~cyl, ncol = 2)
chart_facet_info(p)
chart_facet_info(ggplot(mtcars, aes(wt, mpg)) + geom_point())

}
