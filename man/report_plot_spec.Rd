% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-spec.R
\name{report_plot_spec}
\alias{report_plot_spec}
\title{Draw a custom chart's plot description}
\usage{
report_plot_spec(data, plot, title = NULL)
}
\arguments{
\item{data}{The (transformed) data frame.}

\item{plot}{The plot description (see \code{\link[=report_validate_spec]{report_validate_spec()}}): \code{geom} (line, col, point, area, tile), \code{x}, \code{y},
and optionally \code{colour}, \code{fill}, \code{facet}, \code{position} (dodge, stack, fill), \code{labels} (\code{list(x, y, title, colour, fill)}), \code{percent} (the y values are percentages, 0 to 100) and \code{flip} (bars go across).}

\item{title}{A title, used when \code{plot$labels$title} isn't given.}
}
\value{
A ggplot.
}
\description{
Generic: a table in, a ggplot out. Chart options (the customize panel) apply to the result like to any other
chart.
}
\examples{
d <- data.frame(region = c("A", "B"), value = c(55, 61))
report_plot_spec(d, list(geom = "col", x = "region", y = "value", percent = TRUE))
}
