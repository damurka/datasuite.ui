% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-deck.R
\name{report_deck_layouts}
\alias{report_deck_layouts}
\title{The layouts of a slide}
\usage{
report_deck_layouts(lang = NULL)
}
\arguments{
\item{lang}{\code{NULL} for the names in the three languages (\code{list(en, fr, pt)}), or \code{"en"}, \code{"fr"}, \code{"pt"} for one.}
}
\value{
A list of layouts, each \code{list(id, name, items)}; each item is \code{list(role, type, x, y, w, h)} with \code{role}
\code{"title"}, \code{"subtitle"}, \code{"heading"}, \code{"body"} or \code{"picture"} (a picture placeholder: nothing is written for it
until a picture is put in it), and \code{type} \code{"text"} (a text box) or \code{"content"} (a text, chart, table or picture).
}
\description{
Where a new slide's placeholders go, as PowerPoint places them: each layout lists its boxes as fractions (0 to 1) of
the slide's width and height, so they fit a 16:9 or a 4:3 slide.
}
