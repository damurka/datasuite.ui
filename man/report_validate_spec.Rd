% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-spec.R
\name{report_validate_spec}
\alias{report_validate_spec}
\title{Check a custom chart description}
\usage{
report_validate_spec(spec, members = NULL)
}
\arguments{
\item{spec}{The description: \code{list(title, data = list(member, args), transform, plot)}. \code{transform} is a list of
one-entry lists, each one of \code{filter} (\code{list(column = values)}), \code{select} (column names), \code{rename}
(\code{list(new = "old")}), \code{mutate_ratio} (\code{list(name, numerator, denominator, percent)}), \code{aggregate}
(\code{list(by, fun, cols)}; \code{fun} one of sum, mean, median, min, max), \code{pivot_longer}
(\code{list(cols, names_to, values_to)}), \code{arrange} (\code{list(by, desc)}), \code{top_n} (\code{list(n, by, desc)}). \code{plot} is
\code{list(geom, x, y, colour, fill, facet, position, labels, percent, flip)} with \code{geom} one of line, col, point,
area, tile.}

\item{members}{The names \code{data$member} may take, or \code{NULL} not to check it.}
}
\value{
The spec, tidied. Stops with a message saying what is wrong.
}
\description{
A custom chart (report kind \code{"custom_chart"}) is described by data, never code: where its table comes from, a few
fixed transforms and a plot description. \code{report_validate_spec()} checks one before it is saved, and returns it
tidied (lists from JSON turned into vectors where a vector is meant).
}
\examples{
report_validate_spec(list(
  title = "Coverage by region",
  data = list(member = "calculate_coverage", args = list(admin_level = "adminlevel_1")),
  transform = list(list(filter = list(year = 2023))),
  plot = list(geom = "col", x = "adminlevel_1", y = "value")
))
}
