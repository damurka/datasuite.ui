% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-context.R
\name{report_context}
\alias{report_context}
\title{A report context: one dataset's side of the report builder}
\usage{
report_context(
  draw = NULL,
  asset_get = NULL,
  asset_set = NULL,
  chart_options = NULL,
  fields = NULL,
  years = NULL,
  regions = NULL,
  flag = NULL
)
}
\arguments{
\item{draw}{\verb{function(block, i18n)} returning a ggplot or a flextable for a chart or table block (an error when it
cannot be drawn).}

\item{asset_get}{\verb{function(id)} returning a kept picture or template, \verb{list(type, data = <raw>)}, or \code{NULL}.}

\item{asset_set}{\verb{function(id, value)} keeping one.}

\item{chart_options}{\verb{function()} returning the chart options saved with the data: a named list, \code{"default"} for
every chart and one per chart id or type of graph (\code{\link[=cd_chart_type]{cd_chart_type()}}).}

\item{fields}{\verb{function(project, lang)} returning a named list of field values (e.g. \code{country}).}

\item{years}{\verb{function()} returning the years the data covers.}

\item{regions}{\verb{function()} returning the regions a report can be about.}

\item{flag}{\verb{function()} returning the path of a flag picture for the cover, or \code{NULL}.}
}
\value{
An object of class \code{report_context}.
}
\description{
Each argument is a function; those left out do nothing (no pictures kept, no saved chart options, no fields).
}
