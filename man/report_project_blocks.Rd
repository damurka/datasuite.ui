% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-builder.R
\name{report_project_blocks}
\alias{report_project_blocks}
\title{Every block of a report}
\usage{
report_project_blocks(project)
}
\arguments{
\item{project}{A report (\code{kind = "deck"} for a slide deck; anything else is a document).}
}
\value{
A list of blocks.
}
\description{
A document's blocks, or every item's block of a slide deck, in order, so a caller can count, check or draw them
the same way for both. A slide item's block gets the item's id and its box (\code{box = c(width, height)}, inches), which
\code{\link[=report_block_size]{report_block_size()}} draws it at. A document's free-layout page (a block of type \code{"canvas"}, whose \code{items} are
placed like a slide's) is given as its items' blocks, in the same way; the canvas block itself is not in the list.
}
