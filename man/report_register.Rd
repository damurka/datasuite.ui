% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-context.R
\name{report_register}
\alias{report_register}
\title{Register what an app's reports offer}
\usage{
report_register(
  themes = NULL,
  default_theme = NULL,
  cover = NULL,
  kinds = NULL,
  presets = NULL,
  indicator_name = NULL,
  fields = NULL
)
}
\arguments{
\item{themes}{A named list of designs (see \code{\link[=report_default_design]{report_default_design()}}), each with \code{name} (a string, or a list of
\code{en}, \code{fr}, \code{pt}): offered beside the built-in themes, first.}

\item{default_theme}{The id of the theme new reports start from.}

\item{cover}{A list of cover fields that new reports start from (see \code{\link[=report_default_cover]{report_default_cover()}}), e.g. a \code{kicker}.}

\item{kinds}{A function returning the kinds of chart and table, a named list of \code{\link[=report_kind]{report_kind()}}.}

\item{presets}{A function of \code{lang} returning the standard reports (see the app's documentation of its own).}

\item{indicator_name}{A function of \code{indicator} and \code{i18n} returning an indicator's name in the report's language,
for \code{{chart_indicator}}.}

\item{fields}{A list of extra fields for \code{\link[=report_field_catalog]{report_field_catalog()}}, each \code{list(key, group, label)}; their values come
from the report context's \code{fields()}.}
}
\value{
Invisibly, \code{NULL}.
}
\description{
Called once by an app package (usually in its \code{.onLoad()}); later calls replace only what they give.
}
