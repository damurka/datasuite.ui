% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-options.R
\name{cd_chart_options}
\alias{cd_chart_options}
\title{Chart options}
\usage{
cd_chart_options(
  ...,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  tag = NULL,
  x_title = NULL,
  y_title = NULL,
  legend_title = NULL,
  title_wrap = NULL,
  font_family = NULL,
  text_scale = NULL,
  line_height = NULL,
  title_size = NULL,
  subtitle_size = NULL,
  caption_size = NULL,
  axis_title_size = NULL,
  axis_text_size = NULL,
  x_title_size = NULL,
  y_title_size = NULL,
  x_text_size = NULL,
  y_text_size = NULL,
  legend_title_size = NULL,
  legend_text_size = NULL,
  strip_text_size = NULL,
  title_face = NULL,
  subtitle_face = NULL,
  caption_face = NULL,
  axis_title_face = NULL,
  axis_text_face = NULL,
  legend_title_face = NULL,
  legend_text_face = NULL,
  strip_text_face = NULL,
  text_color = NULL,
  title_color = NULL,
  subtitle_color = NULL,
  caption_color = NULL,
  axis_title_color = NULL,
  axis_text_color = NULL,
  legend_title_color = NULL,
  legend_text_color = NULL,
  strip_text_color = NULL,
  title_align = NULL,
  subtitle_align = NULL,
  caption_align = NULL,
  title_position = NULL,
  caption_position = NULL,
  x_text_angle = NULL,
  y_text_angle = NULL,
  plot_margin = NULL,
  legend_position = NULL,
  legend_direction = NULL,
  legend_justification = NULL,
  legend_ncol = NULL,
  legend_nrow = NULL,
  legend_reverse = NULL,
  legend_key_size = NULL,
  legend_background = NULL,
  legend_labels = NULL,
  legend_label_wrap = NULL,
  category_labels = NULL,
  category_label_wrap = NULL,
  x_limits = NULL,
  y_limits = NULL,
  x_labels = NULL,
  y_labels = NULL,
  axis_line = NULL,
  axis_line_color = NULL,
  axis_ticks = NULL,
  flip = NULL,
  grid = NULL,
  grid_minor = NULL,
  grid_color = NULL,
  grid_linewidth = NULL,
  grid_linetype = NULL,
  panel_border = NULL,
  panel_border_color = NULL,
  panel_color = NULL,
  background_color = NULL,
  strip_background = NULL,
  theme_preset = NULL,
  colors = NULL,
  line_scale = NULL,
  point_scale = NULL,
  alpha = NULL,
  bar_width = NULL,
  label_size = NULL,
  label_angle = NULL,
  label_color = NULL,
  facet_ncol = NULL,
  facet_nrow = NULL,
  facet_scales = NULL,
  strip_position = NULL,
  show_title = NULL,
  show_subtitle = NULL,
  show_caption = NULL,
  show_x_title = NULL,
  show_y_title = NULL,
  show_x_text = NULL,
  show_y_text = NULL,
  show_legend = NULL,
  show_legend_title = NULL,
  show_labels = NULL,
  show_strips = NULL
)
}
\arguments{
\item{...}{The names older plot methods used: \code{x_axis}, \code{x_label} (= \code{x_title}), \code{y_axis}, \code{y_label} (= \code{y_title}),
\code{legend}, \code{fill_label} (= \code{legend_title}), \code{colours} (= \code{colors}).}

\item{title, subtitle, caption, tag}{Chart texts.}

\item{x_title, y_title}{Axis titles.}

\item{legend_title}{Legend title.}

\item{title_wrap}{Wrap the title, subtitle and caption after this many characters.}

\item{font_family}{A font family available on the machine drawing the chart (e.g. \code{"Arial"}, \code{"serif"}).}

\item{text_scale}{A multiplier for every text size in the chart (\code{1.25} = 25 percent larger). Applied before the
explicit sizes below.}

\item{line_height}{Line spacing of text (a multiple of the font size, default about \code{0.9}).}

\item{title_size, subtitle_size, caption_size, axis_title_size, axis_text_size, legend_title_size, legend_text_size, strip_text_size}{Text sizes in points. \verb{axis_*} sets both axes; use the \code{x_}/\code{y_} ones below to set one.}

\item{x_title_size, y_title_size, x_text_size, y_text_size}{Size of one axis' title or tick labels.}

\item{title_face, subtitle_face, caption_face, axis_title_face, axis_text_face, legend_title_face, legend_text_face, strip_text_face}{One of \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}.}

\item{text_color}{Colour of all text; the \verb{*_color} fields below override it for one kind of text.}

\item{title_color, subtitle_color, caption_color, axis_title_color, axis_text_color, legend_title_color, legend_text_color, strip_text_color}{Colour of one kind of text.}

\item{title_align, subtitle_align, caption_align}{One of \code{"left"}, \code{"center"}, \code{"right"}.}

\item{title_position, caption_position}{\code{"panel"} aligns the text with the plotting area, \code{"plot"} with the whole chart.}

\item{x_text_angle, y_text_angle}{Angle of the tick labels in degrees (e.g. \code{45}, \code{90}); the alignment is adjusted so the
labels stay attached to the axis.}

\item{plot_margin}{Space around the chart in points: one number, or four (top, right, bottom, left).}

\item{legend_position}{One of \code{"right"}, \code{"bottom"}, \code{"top"}, \code{"left"}, \code{"none"}.}

\item{legend_direction}{\code{"horizontal"} or \code{"vertical"}.}

\item{legend_justification}{Where the legend sits along its side: \code{"center"}, \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}.}

\item{legend_ncol, legend_nrow}{Number of columns or rows the legend entries are laid out in.}

\item{legend_reverse}{\code{TRUE} to reverse the order of the legend entries.}

\item{legend_key_size}{Size of the legend keys in millimetres.}

\item{legend_background}{Fill colour of the legend box.}

\item{legend_labels}{A named character vector (or list): names are the legend entries the plot draws (or the text
currently shown for them), values the text to show instead.}

\item{legend_label_wrap}{Wrap the legend entries after this many characters.}

\item{category_labels}{The same as \code{legend_labels} for the categories on a discrete axis.}

\item{category_label_wrap}{Wrap the category labels after this many characters.}

\item{x_limits, y_limits}{Two numbers (\code{NA} leaves an end open): the range to show. Zooms; data is not dropped.}

\item{x_labels, y_labels}{How numbers on a continuous axis are written: \code{"number"}, \code{"comma"}, \code{"percent"} (0.5 becomes
50\%), \code{"percent_points"} (50 becomes 50\%), \code{"scientific"}, \code{"compact"} (1.2K, 3M).}

\item{axis_line}{\code{TRUE}/\code{FALSE} to draw or hide the axis lines.}

\item{axis_line_color}{Colour of the axis lines.}

\item{axis_ticks}{\code{TRUE}/\code{FALSE} to draw or hide the tick marks.}

\item{flip}{\code{TRUE} to swap the axes, \code{FALSE} to keep the chart as drawn, \code{NULL} for automatic.}

\item{grid}{Which grid lines to show: \code{"both"}, \code{"horizontal"}, \code{"vertical"} or \code{"none"}.}

\item{grid_minor}{\code{TRUE}/\code{FALSE} to show or hide the minor grid lines.}

\item{grid_color, grid_linewidth, grid_linetype}{Style of the grid lines (\code{linetype} is one of \code{"solid"}, \code{"dashed"},
\code{"dotted"}, \code{"dotdash"}, \code{"longdash"}, \code{"twodash"}).}

\item{panel_border}{\code{TRUE}/\code{FALSE} to draw or hide a border around the plotting area.}

\item{panel_border_color}{Colour of that border.}

\item{panel_color}{Fill colour of the plotting area.}

\item{background_color}{Fill colour of the whole chart.}

\item{strip_background}{Fill colour of facet strips.}

\item{theme_preset}{Start from a different look before the other options are applied: \code{"grey"}, \code{"minimal"},
\code{"classic"}, \code{"bw"}, \code{"light"}, \code{"linedraw"}, \code{"dark"}, \code{"void"}. It replaces the plot's own theme.}

\item{colors}{A named character vector of colours for a colour or fill legend (names are entries as in
\code{legend_labels}); entries not named keep their colour.}

\item{line_scale, point_scale}{Multiply the width of lines and the size of points (\code{1.5} = 50 percent bigger).}

\item{alpha}{Transparency of every mark, from \code{0} (invisible) to \code{1}.}

\item{bar_width}{Width of the bars (and boxes) as a share of the space each has, above \code{0} and up to \code{1}
(\code{0.9} is ggplot2's default).}

\item{label_size}{Size in points of text drawn on the chart (data labels).}

\item{label_angle, label_color}{Angle and colour of that text.}

\item{facet_ncol, facet_nrow}{For a chart split into panels with \code{facet_wrap()}: the number of columns or rows of panels
(a whole number from 1 to 20). Setting only one lets ggplot2 work out the other. Charts split with \code{facet_grid()} keep
their layout (one row or column per value).}

\item{facet_scales}{Whether panels share their axes: \code{"fixed"} (all the same), \code{"free"} (each its own), \code{"free_x"}
or \code{"free_y"} (only that axis varies). Ignored on maps and other charts whose coordinates cannot vary by panel.}

\item{strip_position}{Where the panel labels (strips) go: \code{"top"}, \code{"bottom"}, \code{"left"} or \code{"right"}. With
\code{facet_grid()}, \code{"top"}/\code{"bottom"} place the column labels and \code{"left"}/\code{"right"} the row labels.
Use \code{\link[=chart_facet_info]{chart_facet_info()}} to see whether a chart has panels at all.}

\item{show_title, show_subtitle, show_caption, show_x_title, show_y_title, show_x_text, show_y_text, show_legend, show_legend_title, show_labels, show_strips}{Show or hide one element of the chart: the title, subtitle, caption, an axis title, an axis' tick labels
(\code{show_x_text}, \code{show_y_text}), the whole legend, the legend's title, the data labels drawn on the chart
(\code{geom_text()}, \code{geom_label()} and their ggrepel versions) or the panel names of a chart split into panels
(\code{show_strips}). \code{FALSE} hides it and leaves no space for it; \code{TRUE} shows it again where the chart itself hid it
(a legend the chart put at \code{"none"} goes to the right, a text the chart's theme blanked is drawn), which only works
when the chart has that text at all; \code{NULL} keeps what the chart draws. An empty text (\code{title = ""}, \code{x_title = ""},
\code{legend_title = ""}...) hides that element the same way.}
}
\value{
An object of class \code{cd_chart_options} (a list of the fields that are set).
}
\description{
Describes what a user can change about a chart: its texts, fonts, sizes, colours, angles, legend, axes, grid,
backgrounds and how its lines, points and labels are drawn. Every plot method in the package accepts one through its
\code{options} argument (and any of these fields as a named argument in \code{...}), and an app can keep them per chart, so
the choices survive reloads.
}
\details{
A field left \code{NULL} means "keep what the plot draws by default"; only what is set is changed. Text fields accept \code{""},
which hides that text and the space it took (as the \verb{show_*} fields do). Colours are anything R understands (\code{"red"}, \code{"#1b7837"}, \code{"transparent"}). Sizes are in points
unless said otherwise. \verb{x_*} and \verb{y_*} always mean the data's x and y aesthetics, so they follow the data when the chart
is flipped.
}
\examples{
cd_chart_options(title = "Coverage", legend_position = "bottom", text_scale = 1.2)
cd_chart_options(x_axis = "Year", legend_labels = c(dhis2 = "Routine data"))
cd_chart_options(x_text_angle = 45, grid = "horizontal", grid_color = "grey90", y_labels = "percent_points")

}
\seealso{
\code{\link[=apply_chart_options]{apply_chart_options()}}, \code{\link[=merge_chart_options]{merge_chart_options()}}
}
