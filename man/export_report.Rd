% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-export.R
\name{export_report}
\alias{export_report}
\title{Export a report built in the report builder}
\usage{
export_report(
  context,
  project,
  file,
  format = c("docx", "pdf", "pptx"),
  i18n = NULL,
  subtitle = NULL,
  progress = NULL,
  converter = c("auto", "word", "libreoffice", "browser", "powerpoint")
)
}
\arguments{
\item{context}{A report context (\code{\link[=report_context]{report_context()}}), or what \code{\link[=as_report_context]{as_report_context()}} turns into one.}

\item{project}{The report: \code{list(name, design, cover, blocks)} (such as the app's standard reports, see
\code{\link[=report_register]{report_register()}}), or a slide deck.}

\item{file}{The file to write.}

\item{format}{\code{"docx"} or \code{"pdf"} for a document; \code{"pptx"} or \code{"pdf"} for a slide deck (by default \code{"docx"} for a
document, \code{"pptx"} for a deck).}

\item{i18n}{An object with a \code{t(key)} method, or \code{NULL}.}

\item{subtitle}{Not used any more: the cover page has its own subtitle. Kept so older calls work.}

\item{progress}{A function called with a number between 0 and 1 as the export advances, or \code{NULL}.}

\item{converter}{\code{"auto"} (Word, else LibreOffice, else a browser), or one of \code{"word"}, \code{"libreoffice"}, \code{"browser"};
for a deck \code{"auto"}, \code{"powerpoint"} or \code{"libreoffice"} (\code{"word"} is taken as PowerPoint).}
}
\value{
\code{file}, invisibly, with attribute \code{converter}: what made the PDF (\code{"word"}, \code{"libreoffice"}, \code{"browser"},
\code{"powerpoint"}) or, for Word, what finished the file (\code{"word"}, or \code{NULL}).
}
\description{
Draws every chart and table from the loaded data and writes the report as a Word document or a PDF. The PDF is made
from the Word file (see \code{\link[=report_converter]{report_converter()}}), so the two match page for page. A slide deck (\code{kind = "deck"}) is
written by \code{\link[=export_deck]{export_deck()}}, as a PowerPoint file or a PDF.
}
