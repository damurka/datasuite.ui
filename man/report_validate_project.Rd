% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-spec.R
\name{report_validate_project}
\alias{report_validate_project}
\title{Check a report before it is saved}
\usage{
report_validate_project(project, kinds = NULL, members = NULL)
}
\arguments{
\item{project}{\code{list(name, design, cover, blocks)}.}

\item{kinds}{The report kinds charts and tables may use (names), or \code{NULL} not to check them.}

\item{members}{Passed to \code{\link[=report_validate_spec]{report_validate_spec()}} for custom charts.}
}
\value{
The project, tidied (each block has an id). Stops with a message saying what is wrong.
}
\description{
Checks the shape of a report project (see \code{\link[=export_report]{export_report()}}) -- one the AI built, say -- before an app saves it:
each block's type, and for charts and tables their kind. A \code{custom_chart} block may carry its description in
\code{spec} or, as the AI bridge sends it, in \code{options}; either way it is checked with \code{\link[=report_validate_spec]{report_validate_spec()}} and kept
in \code{spec}.
}
\examples{
report_validate_project(list(name = "Notes", blocks = list(
  list(type = "heading", text = "Summary"),
  list(type = "paragraph", text = "Coverage rose.")
)))
}
