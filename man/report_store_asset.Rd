% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-export.R
\name{report_store_asset}
\alias{report_store_asset}
\title{Store a picture for the reports}
\usage{
report_store_asset(context, id, src)
}
\arguments{
\item{context}{A report context (\code{\link[=report_context]{report_context()}}), or what \code{\link[=as_report_context]{as_report_context()}} turns into one.}

\item{id}{The picture's id.}

\item{src}{A \verb{data:} URL, or an \verb{http(s)://} address.}
}
\value{
A list: \code{id}, \code{ratio} (height / width) and \code{url} (a \verb{data:} URL of the picture as stored), invisibly.
}
\description{
Keeps a picture once in the dataset, from a \verb{data:} URL (as the report builder uploads it) or from a web address
(downloaded now, so the report does not need the internet later). A picture larger than 4000 pixels is made smaller;
formats Word cannot show (WebP, for example) are converted to PNG.
}
